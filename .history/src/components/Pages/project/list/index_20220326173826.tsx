
import './style.scss'


export function ListProject () {

    return (
        <div id="page-list-project">
            <div className="page-list--itens">
                {/* {projects.map(item => (
            <Item
                selecionaProjeto={selecionaProjeto}
                key={item.id}
                {...item}
            />
            ))} */}
            </div>
        </div>
    )
}